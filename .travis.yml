language: rust
rust:
  - stable
  - beta
  - nightly

env:
  - FLAGS="--no-default-features"
  - FLAGS=""

matrix:
  include:
    - rust: nightly
      env: FLAGS="--features \"clippy\""
  
script: ./travis.sh
